---
id: docker-hands-on
type: idea
title: Get hands-on with Docker — build an image, multi-stage builds, Compose for local dev
status: considering
priority: medium
topics: [ docker ]
goals: [ ]
resources: [ ]
url:
source:
raindrop_id:
effort: 4h
created: 2026-09-09
updated: 2026-09-09
---

## Why

`docker` is required by `taxonomy/stack.yml` with zero coverage anywhere in the repo, and it's the
prerequisite for `kubernetes` (already `[[nosql-video-primer]]`-adjacent territory but unbuilt at
the container level).

## Notes

Captured during `/groom` as a stack blind spot — no active goal drives this yet.

## Assessments
